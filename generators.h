#ifndef OOPLAB1_GENERATORS_H
#define OOPLAB1_GENERATORS_H

float Linear(int *, int);

float Squared(int *, int);

float Fibonacci(int *, int);

float Reversed(int *, int);

float Combinated(int *, int);

float Sigma(int *, int);

float Polar(int *, int);

float Correlations(int *, int);

float Log(int *, int);

float Arens(int *, int);

#endif //OOPLAB1_GENERATORS_H
